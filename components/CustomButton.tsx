"use client";

import Image from "next/image";
import { type } from "os";

const CustomButton = () => {
  return (
    <button
      disabled={false}
      type={"button"}
      className={`custom-btn`}
      onClick={() => {}}
    ></button>
  );
};

export default CustomButton;
